#include "GraphsWindow.h"
#include <QGraphicsView>
#include <QGraphicsPixmapItem>

GraphsWindow::GraphsWindow(QWidget* parent)
	:QWidget(parent)
{/*
	QGraphicsScene* p_Scene = new QGraphicsScene();
	QGraphicsView* p_View = new QGraphicsView();
    p_View->setVerticalScrollBarPolicy(Qt::ScrollBarAlwaysOn);
    p_View->setHorizontalScrollBarPolicy(Qt::ScrollBarAlwaysOn);
    p_View->setMouseTracking(true);
    p_View->setScene(p_Scene);
    QGraphicsPixmapItem* p_BackGroundPic = new QGraphicsPixmapItem(QPixmap(":/Pictures/pics/1.bmp"));
	p_Scene->addItem(p_BackGroundPic);
	QGridLayout* p_GridLayout = new QGridLayout();
    p_GridLayout->addWidget(p_View, 1, 0);
    setLayout(p_GridLayout);
   */

    //QwtPlot *pPlot = new QwtPlot();
}


GraphsWindow::~GraphsWindow()
{
}
